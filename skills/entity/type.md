# Description
- Source of truth, define only one entity interface.
- response entities must be derived from entity interface using Omit, Pick, etc.

# Template
```ts

interface Entity {
  field1: type;
  field2: type;
  field3: type;
};

```
